## Topis to discuss:

1. Let's use English only the comments

2. Let's make interfaces for every instance
3. What about naming: function name the same as file name?
4. Where to keep css files (centralized in css folder or in every component's folder)
5. What about use object instead of numerous function parameters:
instead of
```function someFunc(
  a,b,c,d,e,f
)
```
use function setupCheckLogic({a,b,c,d,e,f}) as one parameter object


6. What about of keep inside quiz folder all files related to quiz, dashboard -> dashboard etc..?